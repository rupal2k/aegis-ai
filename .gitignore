# Python
venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/

# Environment
.env
.env.local

# Data — exclude everything in data/output by default
data/output/*
# Allow the 5 faker-generated synthetic CSVs (no real PII) so Render skips data generation
!data/output/companies.csv
!data/output/employees.csv
!data/output/telemetry.csv
!data/output/clinical_events.csv
!data/output/training_dataset.csv
# real_user_training.csv stays excluded — may contain actual uploaded user health data

# ML artifacts — committed after local training so Render loads them directly (no build-time training)
mlflow.db
mlruns/

# IDEs
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Internal dev tools — never commit
CLAUDE.md
.claude/
.codex-security-tools/

# User data and credentials — never commit
config/users.json

# Security scan reports — reveal vulnerabilities, keep local only
security_report.json
security_test_results.txt
SECURITY_TEST_SUMMARY.txt
SECURITY_REPORT.md
pip-audit-report-current.json

# Upload test / sample health data — never commit
data/upload_doc_*.csv

# Wrong-project scripts (Vantage Fit Obsidian tools)
fix_graph.py
graph_check.py

# Internal dev artifacts
.design-package/chats/
.design-package/project/SKILL.md
design.md
